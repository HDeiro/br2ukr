{
    "name": "com.hugodeiro.brlinukr",
    "displayName": "BrInUkr",
    "version": "1.0.0",
    "description": "Simple app to support brazilian people in money conversion on Ukraine",
    "scripts": {
        "dev-js": "babel src/js -d www/js --watch",
        "dev-sass": "sass src/sass/style.scss:www/css/style.css --watch",
        "production": "cross-env NODE_ENV=production babel src/js -d www/js && sass src/sass/style.scss:www/css/style.css --style compressed"
    },
    "author": "Hugo Deiró",
    "license": "Apache-2.0",
    "dependencies": {
        "cordova-android": "^7.1.4",
        "cordova-plugin-whitelist": "^1.3.3"
    },
    "cordova": {
        "plugins": {
            "cordova-plugin-whitelist": {}
        },
        "platforms": [
            "android"
        ]
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-env": "^1.7.0",
        "babel-preset-minify": "^0.5.0",
        "cross-env": "^5.2.0"
    }
}